services:
  app:
    build: .
    restart: unless-stopped
    environment:
      AUTH_USER: ${AUTH_USER}
      AUTH_PASS: ${AUTH_PASS}
      MAX_UPLOAD_MB: ${MAX_UPLOAD_MB:-2048}
      PORT: 8080
    ports:
      - "127.0.0.1:8110:8080"
